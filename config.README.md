# Example Configuration File
# Copy this to config.json and customize for your setup

See config.example.json for the actual configuration template.

HTTPS Configuration:
- enabled: Set to true to enable HTTPS
- port: HTTPS port (default: 3443)
- certificatePath: Path to SSL certificate file
- privateKeyPath: Path to SSL private key file
- redirectHttp: Automatically redirect HTTP to HTTPS
